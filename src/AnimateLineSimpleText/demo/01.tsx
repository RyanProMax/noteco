import React from 'react';
import { AnimateLineSimpleText } from 'noteco';

export default () => (
  <AnimateLineSimpleText
    text="noteco"
    style={{ width: 600, height: 180 }}
    textStyle={{ fontSize: 120, fontWeight: 700 }}
  />
);
